###############################################################################
# << FILE MANAGED BY PUPPET >>
# Manual changes are likey to be overwritten
###############################################################################

<% if scope.lookupvar('pam::pam_mkhomedir') != false then -%>
session	required	pam_mkhomedir.so <%= scope.lookupvar('pam::pam_mkhomedir') %>
<% end -%>

session	[default=1]	pam_permit.so
session	requisite	pam_deny.so
session	required	pam_permit.so
session	required	pam_unix.so 
<% if scope.lookupvar('pam::module_type') == 'ldap' then -%>
session	optional	pam_ldap.so 
<% end -%>
